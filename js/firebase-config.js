// ================================================================
//  YOGA VINAYAGAR TEXTILES — Firebase Configuration
//  Replace the firebaseConfig values below with YOUR Firebase
//  project credentials after creating a project at:
//  https://console.firebase.google.com
// ================================================================

// STEP 1: Go to https://console.firebase.google.com
// STEP 2: Click "Add project" → Name it "yoga-vinayagar"
// STEP 3: Go to Project Settings → "Your apps" → Web app (</>)
// STEP 4: Register app → Copy the firebaseConfig object below

const firebaseConfig = {
  apiKey:            "REPLACE_WITH_YOUR_API_KEY",
  authDomain:        "REPLACE_WITH_YOUR_AUTH_DOMAIN",
  projectId:         "REPLACE_WITH_YOUR_PROJECT_ID",
  storageBucket:     "REPLACE_WITH_YOUR_STORAGE_BUCKET",
  messagingSenderId: "REPLACE_WITH_YOUR_MESSAGING_SENDER_ID",
  appId:             "REPLACE_WITH_YOUR_APP_ID"
};

export default firebaseConfig;
